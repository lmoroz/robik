# TODO

## 1. Подготовка проекта

- [x] Инициализировать `Node.js + TypeScript` проект.
- [x] Добавить базовые npm-скрипты: `dev`, `build`, `start`, `check`.
- [x] Настроить `tsconfig.json`.
- [x] Подключить библиотеку для Telegram Bot API с `polling`.
- [x] Подключить HTTP-клиент для вызова Ollama.
- [x] Добавить `.env.example`.
- [x] Написать `Dockerfile` для контейнеризации бота.

## 2. Конфигурация

- [x] Описать обязательные переменные окружения.
- [x] Сделать fail-fast валидацию конфига при старте.
- [x] Зафиксировать модель по умолчанию.

## 3. Интеграция с Ollama

- [x] Реализовать модуль `llm` для отправки prompt в Ollama.
- [x] Нормализовать ответ модели в обычную строку.
- [x] Обработать таймауты и сетевые ошибки.
- [x] Вернуть контролируемую ошибку, если LLM недоступна.

## 4. Интеграция с Telegram

- [x] Поднять бота в режиме `polling`.
- [x] Обрабатывать только текстовые сообщения.
- [x] На каждый текстовый update вызывать `llm`.
- [x] Отправлять результат обратно в тот же чат.

## 5. Базовая устойчивость

- [x] Проверить несколько сообщений подряд.
- [x] Убедиться, что между сообщениями не хранится состояние диалога.
- [x] Убедиться, что падение LLM не валит весь процесс.
- [x] Добавить минимальное логирование ошибок.

## 6. Бонусы

- [ ] Добавить `/start`.
- [ ] Добавить `SYSTEM_PROMPT`.
- [ ] Добавить выбор модели через команду, только если это не усложнит код непропорционально.

## 7. Проверка задания

- [ ] Бот отвечает на любые текстовые сообщения.
- [ ] Используется локальная LLM, а не облачная.
- [ ] Используется `polling`, а не `webhook`.
- [ ] История диалога не хранится.
- [ ] База данных отсутствует.
- [ ] Ошибка недоступности LLM обрабатывается корректно.
- [ ] README содержит понятную инструкцию по запуску.
- [ ] В README или отдельной заметке зафиксировано, какую модель использовали.
- [ ] В README или отдельной заметке зафиксировано, как именно код генерировался с помощью LLM.

## 8. Ручной smoke-test

- [ ] Запустить `Ollama`.
- [ ] Проверить, что выбранная модель скачана локально.
- [ ] Запустить бота локально.
- [ ] Отправить простое текстовое сообщение в Telegram.
- [ ] Проверить, что бот вернул ответ.
- [ ] Остановить Ollama и проверить сообщение об ошибке.
- [ ] Снова поднять Ollama и проверить повторный успешный запрос.

## 9. Docker smoke-test

- [ ] Собрать образ: `docker build -t telegram-llm-bot .`
- [ ] Убедиться, что `OLLAMA_BASE_URL` в `.env` указывает на хостовый Ollama (`host.docker.internal` или `172.17.0.1`).
- [ ] Запустить контейнер: `docker run --env-file .env telegram-llm-bot`
- [ ] Отправить сообщение в Telegram и убедиться, что бот отвечает.
- [ ] Убедиться, что контейнер не требует установки Node.js на хосте.

