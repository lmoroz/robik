# TODO

## 1. Подготовка проекта

- [ ] Инициализировать `Node.js + TypeScript` проект.
- [ ] Добавить базовые npm-скрипты: `dev`, `build`, `start`, `check`.
- [ ] Настроить `tsconfig.json`.
- [ ] Подключить библиотеку для Telegram Bot API с `polling`.
- [ ] Подключить HTTP-клиент для вызова Ollama.
- [ ] Добавить `.env.example`.

## 2. Конфигурация

- [ ] Описать обязательные переменные окружения.
- [ ] Сделать fail-fast валидацию конфига при старте.
- [ ] Зафиксировать модель по умолчанию.

## 3. Интеграция с Ollama

- [ ] Реализовать модуль `llm` для отправки prompt в Ollama.
- [ ] Нормализовать ответ модели в обычную строку.
- [ ] Обработать таймауты и сетевые ошибки.
- [ ] Вернуть контролируемую ошибку, если LLM недоступна.

## 4. Интеграция с Telegram

- [ ] Поднять бота в режиме `polling`.
- [ ] Обрабатывать только текстовые сообщения.
- [ ] На каждый текстовый update вызывать `llm`.
- [ ] Отправлять результат обратно в тот же чат.

## 5. Базовая устойчивость

- [ ] Проверить несколько сообщений подряд.
- [ ] Убедиться, что между сообщениями не хранится состояние диалога.
- [ ] Убедиться, что падение LLM не валит весь процесс.
- [ ] Добавить минимальное логирование ошибок.

## 6. Бонусы

- [ ] Добавить `/start`.
- [ ] Добавить `SYSTEM_PROMPT`.
- [ ] Добавить выбор модели через команду, только если это не усложнит код непропорционально.

## 7. Проверка задания

- [ ] Бот отвечает на любые текстовые сообщения.
- [ ] Используется локальная LLM, а не облачная.
- [ ] Используется `polling`, а не `webhook`.
- [ ] История диалога не хранится.
- [ ] База данных отсутствует.
- [ ] Ошибка недоступности LLM обрабатывается корректно.
- [ ] README содержит понятную инструкцию по запуску.
- [ ] В README или отдельной заметке зафиксировано, какую модель использовали.
- [ ] В README или отдельной заметке зафиксировано, как именно код генерировался с помощью LLM.

## 8. Ручной smoke-test

- [ ] Запустить `Ollama`.
- [ ] Проверить, что выбранная модель скачана локально.
- [ ] Запустить бота локально.
- [ ] Отправить простое текстовое сообщение в Telegram.
- [ ] Проверить, что бот вернул ответ.
- [ ] Остановить Ollama и проверить сообщение об ошибке.
- [ ] Снова поднять Ollama и проверить повторный успешный запрос.

