# TODO

## 1. Подготовка проекта

- [x] Инициализировать `Node.js + TypeScript` проект.
- [x] Добавить базовые npm-скрипты: `dev`, `build`, `start`, `check`.
- [x] Настроить `tsconfig.json`.
- [x] Подключить библиотеку для Telegram Bot API с `polling`.
- [x] Подключить HTTP-клиент для вызова Ollama.
- [x] Добавить `.env.example`.
- [x] Написать `Dockerfile` для контейнеризации бота.

## 2. Конфигурация

- [x] Описать обязательные переменные окружения.
- [x] Сделать fail-fast валидацию конфига при старте.
- [x] Зафиксировать модель по умолчанию.

## 3. Интеграция с Ollama

- [x] Реализовать модуль `llm` для отправки prompt в Ollama.
- [x] Нормализовать ответ модели в обычную строку.
- [x] Обработать таймауты и сетевые ошибки.
- [x] Вернуть контролируемую ошибку, если LLM недоступна.

## 4. Интеграция с Telegram

- [x] Поднять бота в режиме `polling`.
- [x] Обрабатывать только текстовые сообщения.
- [x] На каждый текстовый update вызывать `llm`.
- [x] Отправлять результат обратно в тот же чат.

## 5. Базовая устойчивость

- [x] Проверить несколько сообщений подряд.
- [x] Убедиться, что между сообщениями не хранится состояние диалога.
- [x] Убедиться, что падение LLM не валит весь процесс.
- [x] Добавить минимальное логирование ошибок.

## 6. Бонусы

- [x] Добавить `/start`.
- [x] Добавить `SYSTEM_PROMPT=YES` , тогда llm-агенту будет отправляться в качестве системного промпта содержимое файла SYSTEM_PROMPT.md , но только если он есть!.
- [x] Добавить выбор модели через команду, только если это не усложнит код непропорционально. Но уточнить, как при этом оставить соответствие требованиям отсутствия БД, памяти и т.п.…

## 7. Проверка задания

- [x] Бот отвечает на любые текстовые сообщения.
- [x] Используется локальная LLM, а не облачная.
- [x] Используется `polling`, а не `webhook`.
- [x] История диалога не хранится.
- [x] База данных отсутствует.
- [x] Ошибка недоступности LLM обрабатывается корректно.
- [x] README содержит понятную инструкцию по запуску.

## 8. Ручной smoke-test

- [x] Запустить `Ollama`.
- [x] Проверить, что выбранная модель скачана локально.
- [x] Запустить бота локально.
- [x] Отправить простое текстовое сообщение в Telegram.
- [x] Проверить, что бот вернул ответ.
- [x] Остановить Ollama и проверить сообщение об ошибке.
- [x] Снова поднять Ollama и проверить повторный успешный запрос.

## 9. Docker smoke-test

- [x] Собрать образ: `docker build -t telegram-llm-bot .`
- [x] Убедиться, что `OLLAMA_BASE_URL` в `.env` указывает на хостовый Ollama (`host.docker.internal` или `172.17.0.1`).
- [x] Запустить контейнер: `docker run --env-file .env telegram-llm-bot`
- [x] Отправить сообщение в Telegram и убедиться, что бот отвечает.
- [x] Убедиться, что контейнер не требует установки Node.js на хосте.

